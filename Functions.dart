 void sayHello(String name){//void 타입은 값을 반환하지 않는다.
  print("안녕 $name야 만나서 반가워");
  //return 
 }

  String sayHello(String name){
  return "안녕 $name야 만나서 반가워";
 }

  String sayHello1(String name)
  => "안녕 $name야 만나서 반가워";

  num plus(num a,num b) =>  a + b;//=> 는 중괄호나 return을 쓸필요 없음
 

 void main(){//void 타입은 값을 반환하지 않는다.

 }